[Unit]
Description=Ladder web proxy
After=network.target

[Service]
Type=simple
EnvironmentFile=/etc/ladder.env
ExecStart=/usr/local/bin/ladder
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
