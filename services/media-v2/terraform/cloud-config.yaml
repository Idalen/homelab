#cloud-config
package_update: true
package_upgrade: true

packages:
  - qemu-guest-agent
  - ca-certificates
  - curl
  - gnupg
  - bash
  - usbutils

runcmd:
  - systemctl enable --now qemu-guest-agent
